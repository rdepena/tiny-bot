Dependencies: 
pkg-config - brew install pkg-config
sdl2 - brew install sdl2

TODO: 
* Split serial code into it's own file
* Add Data smoothing 
* Add fonts and distance data to the matrix
* Improve matrix code 